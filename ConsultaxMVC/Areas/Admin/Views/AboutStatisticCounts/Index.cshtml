@model IEnumerable<ConsultaxMVC.Models.AboutStatisticCount>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.countNum)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TitleLeft)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TitleRight)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.countNum)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TitleLeft)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TitleRight)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
